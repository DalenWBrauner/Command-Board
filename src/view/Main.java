package view;

import javafx.application.Application;

/*=============================
 * Kind of illustrate the application launching in Java FX, i.e. what our game will probably end up looking like pre launch, w/out much logic in Game Class
 *=============================
public class Main extends Application{

  static InterfaceControl controller;
  static InterfaceView viewer;
  
  public static void main(String[]args){
  
  controller = new CommandBoardController();
  viewer = new CommandBoardView(controller);
  
  //Launch game
  Launch(args);
  }
  
  public void start(Stage primaryStage)
  {
     try{
	    viewer.start(primaryStage);
		} catch (Exception e) {
		e.printStackTrace();
		}
    }
}
*/
